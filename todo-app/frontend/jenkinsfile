pipeline {
    agent any
    tools {
        nodejs 'NodeJS'   // must match the name in Jenkins Tools
    }

    stages {

        // Stage 1: Pull latest code from GitHub
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/kinlay123/KinlayGyeltshen_02240347_DSO_A1.git',
                    credentialsId: 'github-credentials'
            }
        }

        // Stage 2: Install npm packages
        stage('Install') {
            steps {
                sh 'npm install'
            }
        }

        // Stage 3: Compile/build the app
        stage('Build') {
            steps {
                sh 'npm run build'
            }
        }

        // Stage 4: Run Jest tests and publish results
        stage('Test') {
            steps {
                sh 'npm test'
            }
            post {
                always {
                    junit 'junit.xml'   // publish test report to Jenkins UI
                }
            }
        }

        // Stage 5: Build Docker image and push to Docker Hub
        stage('Deploy') {
            steps {
                script {
                    docker.build('kinlay2005/fe-todo:02240347').push()
                    docker.withRegistry(
                        'https://registry.hub.docker.com',
                        'docker-hub-creds'
                    ) {
                        docker.image('kinlay2005/fe-todo:02240347').push()
                    }
                }
            }
        }
    }
}